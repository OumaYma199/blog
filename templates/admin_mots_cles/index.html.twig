{% extends 'base.html.twig' %}

{% block title %}MotsCles index{% endblock %}

{% block body %}
<section class="home" id="home">
    <div class="home-text" >
       <h1>Blog</h1>
       <span class=".home-subtitle">Découvrez, Inspirez-vous et Partagez Votre Passion avec Notre Communauté</span>
    </div>
</section>

    <table class="table">
        <thead>
            <tr>
                <th>Catégorie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for mots_cle in mots_cles %}
            <tr>
                <td>{{ mots_cle.mot }}</td>
                <td>
                    <a href="{{ path('app_admin_mots_cles_show', {'id': mots_cle.id}) }}">Afficher</a>
                    <a href="{{ path('app_admin_mots_cles_edit', {'id': mots_cle.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_admin_mots_cles_new') }}">Create new</a>
{% endblock %}
